export const adios = "adios";

// (suggestionLib.apiGetSuggestions as jest.Mock).mockImplementationOnce(() =>
//   Promise.resolve(fakeCards)
// );

// (belovedLib.apiAddBeloved as jest.Mock).mockImplementationOnce(() =>
//   Promise.resolve("OK")
// );
